---
import { getCollection } from 'astro:content';
import '../styles/global.css'; 

const allSpots = await getCollection('spots');
allSpots.sort((a, b) => a.data.distance - b.data.distance);
---

<html lang="ko">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width" />
		<title>지리산 생태탐방원 인근 관광지</title>
	</head>
	<body class="bg-slate-50 text-slate-800 font-sans">
        
		<header class="bg-white border-b border-slate-200 sticky top-0 z-50">
            <div class="max-w-md mx-auto px-4 py-3 flex justify-between items-center">
			    <h1 class="text-lg font-bold text-slate-800">
                    ⛰️ 지리산 인근 관광지
                </h1>
                <span class="text-xs text-slate-500 bg-slate-100 px-2 py-1 rounded">탐방원 기준</span>
            </div>
		</header>

        <main class="max-w-md mx-auto p-4 pb-20">
            
            <div class="relative mb-6">
                <input type="text" id="searchInput" placeholder="관광지 이름 검색" 
                    class="w-full p-3 pl-10 bg-white border border-slate-300 rounded-lg text-sm focus:outline-none focus:border-slate-500 transition-colors">
                <span class="absolute left-3 top-3.5 text-slate-400">🔍</span>
            </div>

            <div class="flex border-b border-slate-200 mb-4">
                <button class="main-tab active flex-1 py-2 text-sm font-bold text-slate-400 border-b-2 border-transparent hover:text-slate-600" data-mode="distance">📏 거리순</button>
                <button class="main-tab flex-1 py-2 text-sm font-bold text-slate-400 border-b-2 border-transparent hover:text-slate-600" data-mode="category">🏷️ 종류별</button>
                <button class="main-tab flex-1 py-2 text-sm font-bold text-slate-400 border-b-2 border-transparent hover:text-slate-600" data-mode="region">📍 지자체별</button>
            </div>

            <div id="sub-filter-container" class="mb-6 hidden overflow-x-auto whitespace-nowrap pb-2 no-scrollbar"></div>

            <!-- 이미지 제거된 리스트 -->
            <div id="spot-list" class="space-y-3">
                {allSpots.map((spot) => (
                    <a href={`/spots/${spot.slug}`} class="block">
                        <article class="spot-card bg-white rounded-lg border border-slate-200 p-4 hover:shadow-md transition-shadow"
                            data-title={spot.data.title}
                            data-category={spot.data.category}
                            data-region={spot.data.region}
                            data-distance={spot.data.distance}
                        >
                            <div class="flex justify-between items-start mb-2">
                                <h2 class="text-lg font-bold text-slate-800">{spot.data.title}</h2>
                                <div class="flex gap-1">
                                    <span class="text-xs bg-slate-100 text-slate-600 px-2 py-0.5 rounded">{spot.data.region}</span>
                                    <span class="text-xs bg-slate-100 text-slate-600 px-2 py-0.5 rounded">{spot.data.category}</span>
                                </div>
                            </div>
                            <div class="text-sm font-bold text-green-700 flex items-center gap-1">
                                <span>📍 {spot.data.distance}km</span>
                                <span class="text-xs text-slate-400 font-normal">({spot.data.time})</span>
                            </div>
                        </article>
                    </a>
                ))}
            </div>

            <div id="no-result" class="hidden text-center py-10 text-slate-400">검색 결과가 없습니다.</div>
        </main>

        <style>
            .main-tab.active { color: #1e293b; border-color: #1e293b; }
            .sub-chip { display: inline-block; padding: 6px 12px; margin-right: 8px; font-size: 0.75rem; font-weight: 500; color: #475569; background-color: white; border: 1px solid #cbd5e1; border-radius: 9999px; transition: background-color 0.2s, color 0.2s; cursor: pointer; }
            .sub-chip.active { background-color: #1e293b; color: white; border-color: #1e293b; }
            .no-scrollbar::-webkit-scrollbar { display: none; }
            .no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }
        </style>

        <script>
            const searchInput = document.getElementById('searchInput');
            const container = document.getElementById('spot-list');
            const mainTabs = document.querySelectorAll('.main-tab');
            const subFilterContainer = document.getElementById('sub-filter-container');
            const noResult = document.getElementById('no-result');
            
            let allCards = Array.from(document.querySelectorAll('.spot-card')).map(card => card.closest('a'));
            const categories = ["자연", "문화유산", "국립공원시설", "정원", "기타"]; 
            const regions = ["구례", "하동", "남원", "순천", "함양", "산청", "곡성"]; 

            mainTabs.forEach(tab => {
                tab.addEventListener('click', () => {
                    mainTabs.forEach(t => t.classList.remove('active'));
                    tab.classList.add('active');
                    const mode = tab.getAttribute('data-mode');
                    
                    if (mode === 'distance') {
                        subFilterContainer.classList.add('hidden');
                        resetFilter(); 
                        sortByDistance();
                    } else if (mode === 'category') {
                        renderSubChips(categories, 'category');
                    } else if (mode === 'region') {
                        renderSubChips(regions, 'region');
                    }
                });
            });

            function renderSubChips(items, type) {
                subFilterContainer.classList.remove('hidden');
                subFilterContainer.innerHTML = '';
                const allBtn = document.createElement('button');
                allBtn.className = 'sub-chip active';
                allBtn.innerText = '전체';
                allBtn.onclick = () => {
                    document.querySelectorAll('.sub-chip').forEach(c => c.classList.remove('active'));
                    allBtn.classList.add('active');
                    resetFilter(); 
                };
                subFilterContainer.appendChild(allBtn);

                items.forEach(item => {
                    const btn = document.createElement('button');
                    btn.className = 'sub-chip';
                    btn.innerText = item;
                    btn.onclick = () => {
                        document.querySelectorAll('.sub-chip').forEach(c => c.classList.remove('active'));
                        btn.classList.add('active');
                        filterList(type, item);
                    };
                    subFilterContainer.appendChild(btn);
                });
            }

            function filterList(type, value) {
                let visibleCount = 0;
                allCards.forEach(cardLink => {
                    const card = cardLink.querySelector('.spot-card');
                    const cardValue = card.getAttribute(`data-${type}`);
                    if (cardValue === value) { cardLink.style.display = 'block'; visibleCount++; } 
                    else { cardLink.style.display = 'none'; }
                });
                checkEmpty(visibleCount);
            }

            function resetFilter() {
                allCards.forEach(cardLink => cardLink.style.display = 'block');
                checkEmpty(allCards.length);
            }

            function sortByDistance() {
                const sorted = [...allCards].sort((a, b) => {
                    const distA = parseFloat(a.querySelector('.spot-card').getAttribute('data-distance'));
                    const distB = parseFloat(b.querySelector('.spot-card').getAttribute('data-distance'));
                    return distA - distB;
                });
                container.innerHTML = '';
                sorted.forEach(card => container.appendChild(card));
            }

            searchInput.addEventListener('input', (e) => {
                const keyword = e.target.value.toLowerCase();
                let visibleCount = 0;
                allCards.forEach(cardLink => {
                    const card = cardLink.querySelector('.spot-card');
                    const title = card.getAttribute('data-title').toLowerCase();
                    if (title.includes(keyword)) { cardLink.style.display = 'block'; visibleCount++; } 
                    else { cardLink.style.display = 'none'; }
                });
                checkEmpty(visibleCount);
            });

            function checkEmpty(count) {
                noResult.style.display = count === 0 ? 'block' : 'none';
            }
        </script>
	</body>
</html>