---
import { getCollection } from 'astro:content';
const spots = await getCollection('spots');
---

<html lang="ko">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width" />
		<title>지리산 관광지 안내</title>
	</head>
	<body class="p-5">
		<h1 class="text-3xl font-bold mb-5">⛰️ 지리산 탐방원 인근 관광지</h1>

		<ul class="space-y-4">
			{spots.map((spot) => (
				<li class="border p-4 rounded shadow-sm bg-white">
                    <h2 class="text-xl font-bold text-green-700">{spot.data.title}</h2>
                    <p class="text-gray-600">
                        📍 {spot.data.region} | 🚗 {spot.data.distance}km ({spot.data.time})
                    </p>
                    <p class="mt-2 text-sm text-gray-500">{spot.data.address}</p>
				</li>
			))}
		</ul>
	</body>
</html>