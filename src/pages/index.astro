---
import { getCollection } from 'astro:content';
import '../styles/global.css'; 

const allSpots = await getCollection('spots');

// 기본 정렬: 거리순
allSpots.sort((a, b) => a.data.distance - b.data.distance);
---

<html lang="ko">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width" />
		<title>지리산 생태탐방원 인근 관광지</title>
	</head>
	<body class="bg-slate-50 text-slate-800 font-sans">
        
		<header class="bg-white border-b border-slate-200 sticky top-0 z-50">
            <div class="max-w-md mx-auto px-4 py-3 flex justify-between items-center">
			    <h1 class="text-lg font-bold text-slate-800">
                    ⛰️ 지리산 인근 관광지
                </h1>
                <span class="text-xs text-slate-500 bg-slate-100 px-2 py-1 rounded">탐방원 기준</span>
            </div>
		</header>

        <main class="max-w-md mx-auto p-4 pb-20">
            
            <!-- 통합 검색창 -->
            <div class="relative mb-6">
                <input type="text" id="searchInput" placeholder="관광지 이름 검색" 
                    class="w-full p-3 pl-10 bg-white border border-slate-300 rounded-lg text-sm focus:outline-none focus:border-slate-500 transition-colors">
                <span class="absolute left-3 top-3.5 text-slate-400">🔍</span>
            </div>

            <!-- 1차 탭 (메인 필터) -->
            <div class="flex border-b border-slate-200 mb-4">
                <button class="main-tab active flex-1 py-2 text-sm font-bold text-slate-400 border-b-2 border-transparent hover:text-slate-600" data-mode="distance">
                    📏 거리순
                </button>
                <button class="main-tab flex-1 py-2 text-sm font-bold text-slate-400 border-b-2 border-transparent hover:text-slate-600" data-mode="category">
                    🏷️ 종류별
                </button>
                <button class="main-tab flex-1 py-2 text-sm font-bold text-slate-400 border-b-2 border-transparent hover:text-slate-600" data-mode="region">
                    📍 지자체별
                </button>
            </div>

            <!-- 2차 탭 (하위 필터 - 초기에는 숨김) -->
            <div id="sub-filter-container" class="mb-6 hidden overflow-x-auto whitespace-nowrap pb-2 no-scrollbar">
                <!-- 자바스크립트로 버튼이 들어갈 자리 -->
            </div>

            <!-- 리스트 영역 -->
            <div id="spot-list" class="space-y-3">
                {allSpots.map((spot) => (
                    <a href={`/spots/${spot.slug}`} class="block">
                        <article class="spot-card bg-white rounded-lg border border-slate-200 overflow-hidden flex h-28 transition-transform active:scale-95"
                            data-title={spot.data.title}
                            data-category={spot.data.category}
                            data-region={spot.data.region}
                            data-distance={spot.data.distance}
                        >
                            <!-- 이미지 -->
                            <div class="w-28 bg-slate-200 flex-shrink-0 relative">
                                {spot.data.image ? (
                                    <img src={spot.data.image} alt={spot.data.title} class="w-full h-full object-cover" />
                                ) : (
                                    <div class="w-full h-full flex items-center justify-center text-slate-400 text-2xl">⛰️</div>
                                )}
                            </div>
                            
                            <!-- 정보 -->
                            <div class="flex-1 p-3 flex flex-col justify-between">
                                <div>
                                    <div class="flex gap-1 mb-1">
                                        <span class="text-[10px] bg-slate-100 text-slate-600 px-1.5 py-0.5 rounded">{spot.data.region}</span>
                                        <span class="text-[10px] bg-slate-100 text-slate-600 px-1.5 py-0.5 rounded">{spot.data.category}</span>
                                    </div>
                                    <h2 class="text-base font-bold text-slate-800 leading-tight">{spot.data.title}</h2>
                                </div>
                                <div class="text-sm font-bold text-green-700 flex items-center gap-1">
                                    <span>📍 {spot.data.distance}km</span>
                                    <span class="text-xs text-slate-400 font-normal">({spot.data.time})</span>
                                </div>
                            </div>
                        </article>
                    </a>
                ))}
            </div>

            <div id="no-result" class="hidden text-center py-10 text-slate-400">
                검색 결과가 없습니다.
            </div>

        </main>

        <style>
            .main-tab.active {
                @apply text-slate-800 border-slate-800;
            }
            .sub-chip {
                @apply inline-block px-3 py-1.5 mr-2 text-xs font-medium text-slate-600 bg-white border border-slate-300 rounded-full transition-colors cursor-pointer;
            }
            .sub-chip.active {
                @apply bg-slate-800 text-white border-slate-800;
            }
            .no-scrollbar::-webkit-scrollbar { display: none; }
            .no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }
        </style>

        <script>
            const searchInput = document.getElementById('searchInput');
            const container = document.getElementById('spot-list');
            const mainTabs = document.querySelectorAll('.main-tab');
            const subFilterContainer = document.getElementById('sub-filter-container');
            const noResult = document.getElementById('no-result');
            
            let allCards = Array.from(document.querySelectorAll('.spot-card'));
            const categories = ["자연", "문화유산", "국립공원시설", "기타"]; // 종류 데이터
            const regions = ["구례", "하동", "남원", "순천", "함양", "산청", "곡성"]; // 지자체 데이터

            // 1. 메인 탭 클릭
            mainTabs.forEach(tab => {
                tab.addEventListener('click', () => {
                    // 탭 스타일 변경
                    mainTabs.forEach(t => t.classList.remove('active'));
                    tab.classList.add('active');

                    const mode = tab.getAttribute('data-mode');
                    
                    if (mode === 'distance') {
                        subFilterContainer.classList.add('hidden');
                        resetFilter(); // 필터 해제하고 전체 보이기
                        sortByDistance();
                    } else if (mode === 'category') {
                        renderSubChips(categories, 'category');
                    } else if (mode === 'region') {
                        renderSubChips(regions, 'region');
                    }
                });
            });

            // 2. 하위 칩 생성 함수
            function renderSubChips(items, type) {
                subFilterContainer.classList.remove('hidden');
                subFilterContainer.innerHTML = '';
                
                // '전체' 칩 추가
                const allBtn = document.createElement('button');
                allBtn.className = 'sub-chip active';
                allBtn.innerText = '전체';
                allBtn.onclick = () => {
                    document.querySelectorAll('.sub-chip').forEach(c => c.classList.remove('active'));
                    allBtn.classList.add('active');
                    resetFilter(); // 전체보기
                };
                subFilterContainer.appendChild(allBtn);

                items.forEach(item => {
                    const btn = document.createElement('button');
                    btn.className = 'sub-chip';
                    btn.innerText = item;
                    btn.onclick = () => {
                        document.querySelectorAll('.sub-chip').forEach(c => c.classList.remove('active'));
                        btn.classList.add('active');
                        filterList(type, item);
                    };
                    subFilterContainer.appendChild(btn);
                });
            }

            // 3. 필터링 로직
            function filterList(type, value) {
                let visibleCount = 0;
                allCards.forEach(card => {
                    // 부모인 a 태그를 제어해야 함
                    const cardLink = card.closest('a');
                    const cardValue = card.getAttribute(`data-${type}`);
                    
                    if (cardValue === value) {
                        cardLink.style.display = 'block';
                        visibleCount++;
                    } else {
                        cardLink.style.display = 'none';
                    }
                });
                checkEmpty(visibleCount);
            }

            // 4. 필터 초기화 (전체 보이기)
            function resetFilter() {
                allCards.forEach(card => card.closest('a').style.display = 'block');
                checkEmpty(allCards.length);
            }

            // 5. 거리순 정렬
            function sortByDistance() {
                const sorted = [...allCards].sort((a, b) => {
                    return parseFloat(a.getAttribute('data-distance')) - parseFloat(b.getAttribute('data-distance'));
                });
                // DOM 다시 붙이기 (a 태그 단위로 이동)
                sorted.forEach(card => container.appendChild(card.closest('a')));
            }

            // 6. 검색 기능
            searchInput.addEventListener('input', (e) => {
                const keyword = e.target.value.toLowerCase();
                let visibleCount = 0;
                allCards.forEach(card => {
                    const cardLink = card.closest('a');
                    const title = card.getAttribute('data-title').toLowerCase();
                    if (title.includes(keyword)) {
                        cardLink.style.display = 'block';
                        visibleCount++;
                    } else {
                        cardLink.style.display = 'none';
                    }
                });
                checkEmpty(visibleCount);
            });

            function checkEmpty(count) {
                noResult.style.display = count === 0 ? 'block' : 'none';
            }
        </script>
	</body>
</html>